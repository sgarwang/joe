<%= form_for @event, :url => { :controller => 'events', :action => 'create' } do |f| %>
    
    <%# Use local template _form.html.erb, pass f as local variable%>
    <%= render :partial => 'form', :locals => { :f => f } %>
    
    <%= f.submit "Create" %>
<% end %>
