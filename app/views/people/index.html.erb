<h1>Listing people</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Bio</th>
    <th>Birthday</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<%# Try the logger.debug not show in production. (info/warn/error/fatal) %>
<% Rails.logger.debug("people: #{@people.inspect}") %>

<% @people.each do |person| %>
  <tr>
    <td><%= person.name %></td>
    <td><%= person.bio %></td>
    <td><%= person.birthday %></td>
    <td><%= link_to 'Show', person %></td>
	<%# Change link_to to button_to has Routing Error: POST /people/1/edit, due to jQuery?%>
    <td><%= link_to 'Edit', edit_person_path(person) %></td>
    <td><%= button_to 'Edit', edit_person_path(person) %></td> 
    <td><%= button_to 'Destroy', person, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<%# Learn debug from: http://guides.rubyonrails.org/debugging_rails_applications.html %>
<%= debug @people %>
<p>
  <b>PeopleName:</b>
  <%=h @people.inspect %>
</p>
<br />

<%= link_to 'New Person', new_person_path %>
